#include <stdio.h>

int main(){
    int r = 6;
    int height = 5;
    printf("the area of cicle with radius %d is %f",r,3.14*r*r);
    printf("the volume of cylinder with radius %d and height %d is %f",r,height,3.14*r*r*height);
    return 0;
}