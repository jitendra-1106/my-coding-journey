/*  
Twenty integers are to be stored in memory. What will you prefer- Array or
structure?
*/

/*
If twenty integers are to be stored in memory, I would prefer an array because all 20 values are of the same data type (int).

int arr[20];
*/

#include <stdio.h>

int main(){
    int arr[20];
    return 0;
}