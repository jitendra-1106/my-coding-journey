#include <stdio.h> // input-output ke liye header file
int main(){ // program yahin se start hota hai
    int a;//variable declaration  (memory reserve)
    a=6;//variable initialization (variable ko value di)
    printf("the output of this program is %d",a);// %d ki jagah a ki value (6) print hogi
    //%d ek format specifier hai jo printf ko bolta hai "yahan ek integer (number) print karna hai"
    return 0;// program successfully end
}