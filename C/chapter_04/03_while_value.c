#include <stdio.h>

int main(){
    // The loop variable is also called the loop counter.
    int i=0;

     // The condition is checked before every iteration.
    while(i<11){
        printf("The value of i is %d\n",i);
        i++;
    }
    return 0;
}