#include <iostream>
using namespace std;

int main(){

    int a = 4 , b = 7;
    int c = ((((a*45)+b)-45)+3);

    return 0;
}

/*
// Operator Precedence:
Determines which operator is performed first when different operators are present in an expression.

// Operator Associativity:
Determines the direction of evaluation (left-to-right or right-to-left) when operators have the same precedence.
*/